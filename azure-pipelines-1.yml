trigger:
  - main

pool:
  name:  'myvmsspool'

strategy:
  matrix:
    param1:
      - value1
      - value2
      - value3
    param2:
      - valueA
      - valueB

jobs:
- job: RunTests
  displayName: "Run tests with param1=$(param1) "
  steps:
  - script: echo "Running tests with param1=$(param1)"

- job: RunTests
  displayName: "Run tests with param1=$(param2) "
  steps:
  - script: echo "Running tests with param1=$(param2)"


- job: RunTests
  displayName: "Run tests with param1=$(param3) "
  steps:
  - script: echo "Running tests with param1=$(param3)"