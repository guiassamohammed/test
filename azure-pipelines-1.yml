trigger:
  -  master


strategy:
  matrix:
    param1:
      - value1
      - value2
      - value3
    param2:
      - valueA
      - valueB
pool:
  name:  'myvmsspool'

jobs:
- job: RunTests
  displayName: "Run tests with param1=$(param1) and param2=$(param2)"
  steps:
  - script: echo "Running tests with param1=$(param1) and param2=$(param2)"